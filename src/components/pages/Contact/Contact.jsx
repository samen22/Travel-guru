import { Helmet } from "react-helmet";

const Contact = () => {
    return (
        <div>
            <Helmet>
                <title>Travel Guru | Contact</title>
            </Helmet>
        </div>
    );
};

export default Contact;